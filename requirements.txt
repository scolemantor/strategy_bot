alpaca-py>=0.30.0
pydantic>=2.5.0
pyyaml>=6.0
python-dotenv>=1.0.0
pyarrow>=15.0.0
lxml>=5.0.0
requests>=2.31.0
yfinance>=0.2.40
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
httpx>=0.27.0
# Phase 7.5 Dashboard
sqlalchemy>=2.0.0
alembic>=1.13.0
psycopg2-binary>=2.9.9
bcrypt>=4.1.0
python-jose[cryptography]>=3.3.0
python-multipart>=0.0.9
ruamel.yaml>=0.18.0
# Phase 8a Technical Overlay
# pandas-ta's PyPI release (0.3.14b0) is abandoned and 0.4.x betas need
# Python 3.12+. Install from GitHub development branch instead — the
# active source against pandas 2.x. Requires `git` in the build image
# (added to Dockerfile's apt install). Runtime ImportError fallback in
# scanners/technical_overlay.py degrades the scanner cleanly if the git
# install ever fails (network blip etc) — won't break scan pipeline.
pandas-ta @ git+https://github.com/twopirllc/pandas-ta.git@development
